<?xml version="1.0" encoding="utf-8"?>
<org.alphatilesapps.alphatiles.SquareConstraintLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:id="@+id/tileadapter_tile"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    >

    <LinearLayout
        android:layout_width="match_parent"
        android:layout_height="match_parent"
        android:orientation="vertical"
        android:gravity="bottom"
        android:layout_margin="2dp"
        >

        <TextView
            android:id="@+id/tileadapter_text"
            android:layout_width="match_parent"
            android:layout_height="match_parent"
            android:textAlignment="center"
            android:bufferType="spannable"
            android:textColor="@color/WHITE"
            android:textDirection="rtl"
            android:textSize="0sp" />
    </LinearLayout>

</org.alphatilesapps.alphatiles.SquareConstraintLayout>